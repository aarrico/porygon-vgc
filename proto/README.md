# proto

The canonical gRPC contract between the Go core and the Python analytics service. Single source of truth, code-generated into `internal/platform` and `analytics/`. The Go core is the declared owner of these shapes (AD-8).
