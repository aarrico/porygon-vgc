# coaching

CAP-6 — review decision points from past battles, drilled via SM-2-style spaced repetition.

Depends on `battlesim`'s persisted event log as a read model — never the reverse (AD-2). Governed by AD-1, AD-2, AD-4, AD-6, AD-13 — see [docs/adr/](../../../docs/adr/).
