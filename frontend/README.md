# frontend

Reserved, empty. React Native (web & mobile) — real Phase 2 scope per `vision.md`, not a non-goal, but no capability in SPEC-porygon-vgc requires it yet (v1 is API-only). Nothing governs this directory's internals until that phase actually starts.
